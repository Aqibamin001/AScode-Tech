-- The /internship/apply form and its submission storage have been removed.
-- "Apply Now" now redirects straight to WhatsApp, so this table is no
-- longer needed.
DROP TABLE IF EXISTS public.internship_applications;
